#include <iostream>
#include <vector>

/*
C++0x란 C++언어의 다음 버전에 대한 가명칭을 말한다.
VC++ 2008 : C++03
C++0x : 200x년도
프로그래밍을 간단하게 구현하기 위한 문법 추가와 기존 기능이 강화됨
C++0x는 흔히 모던C++이라고 부르는 C++11로 바뀌게 된다.
C++11부터 Lambda, Rvalue Reference 등 큰 변화가 도입되었다.
*/

int main(void)
{

	/*
	기존에는 아래와 같으면 산괄호가 operator>> 로 해석되서 std::vector<std::basic_string<char> > 이런식으로 사이에
	공백을 넣어줘야 했었는데 그럴 필요가 없어졌다.
	*/
	std::vector<std::basic_string<char>> v;


	
	return 0;
}